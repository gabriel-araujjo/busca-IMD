//
// Created by gabriel on 5/17/16.
//

#ifndef BUSCA_IMD_ARRAY_H
#define BUSCA_IMD_ARRAY_H

/**template <typename Type>
typedef struct tpArray {
    Type * data;
    int size;
} * Array;**/

/**namespace core {
    template <typename Type>
    class Array{
    private:
        typedef struct tpArray {
            Type * data;
            int size;
        } * mArray;

    public:
        Type content = new mArray;
    };
}**/

#endif //BUSCA_IMD_ARRAY_H
